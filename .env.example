# Cookie Butler 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ================================
# 基础配置
# ================================

# 服务器端口（可选，默认3000）
PORT=3000

# 环境标识（development/production）
# 生产环境请设置为 production
NODE_ENV=development

# ================================
# CORS安全配置
# ================================

# 允许的CORS源（重要！用逗号分隔多个域名）
# 生产环境必须配置，只允许你的官方域名访问API
# 示例：
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com

# Vercel部署URL（Vercel会自动设置，无需手动配置）
# VERCEL_URL=your-app.vercel.app

# ================================
# API配置
# ================================

# API请求超时时间（毫秒，可选，默认15000）
# API_TIMEOUT=15000

# 会话密钥过期时间（毫秒，可选，默认300000=5分钟）
# SESSION_TTL=300000

# ================================
# 安全说明
# ================================
# 1. ALLOWED_ORIGINS 是最重要的安全配置
# 2. 生产环境务必设置正确的域名，防止恶意网站盗用API
# 3. 不要在ALLOWED_ORIGINS中使用通配符(*)
# 4. 域名必须包含协议(http://或https://)
# 5. 不要在域名末尾添加斜杠
